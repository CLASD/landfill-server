angular.module('reportService', ['ngCookies'])
	.factory('reportService', function ($resource, $cookies) {

//		var id = $cookies.get('tv_sessionId');
//		return $resource('/api/history', {} ,{
//      query: {method:'GET', params:{sessionId: id}, isArray:true}
//    });
	});
